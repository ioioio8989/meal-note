# MEAL NOTE v20 — Android PWA 설치

## 이번 작업
앱 기능·UI·저장 방식은 변경하지 않았습니다.
v20에 이미 있던 manifest, 192/512 아이콘, standalone 실행 설정, 서비스 워커, 설치 요청 처리를 그대로 사용합니다.
배포에 필요한 앱 파일 12개만 모았습니다. 기존 앱 파일과 모두 동일합니다.

## 먼저 필요한 것
이 폴더의 내용을 HTTPS로 제공되는 웹 공간에 올려야 합니다.
ZIP이나 index.html을 휴대폰에서 파일로 여는 것만으로는 PWA 설치가 되지 않습니다.
PC의 localhost 주소도 휴대폰 설치 주소로 쓸 수 없습니다.
이번 작업에서는 웹 주소 배포·호스팅 가입·외부 서비스 연결을 하지 않았습니다.

이미 사용하는 HTTPS 주소가 있다면 그 주소와 기존 경로를 유지해 앱 파일을 올리세요.
파일들은 같은 폴더에 두고, index.html과 manifest.webmanifest, sw.js 및 아이콘이 로그인이나 다운로드 화면 없이 열려야 합니다.

## Android 설치
1. Android의 Chrome에서 배포된 MEAL NOTE HTTPS 주소를 엽니다.
2. 오른쪽 위 ⋮ 메뉴 → '홈 화면에 추가' → '설치'를 선택합니다. 표시되는 메뉴 이름은 기기에 따라 다를 수 있습니다.
3. 홈 화면의 MEAL NOTE 아이콘으로 실행합니다. standalone 설정으로 일반 브라우저 탭과 분리된 창에서 열립니다.
앱의 설정 → 기록 보관 · 데이터 관리에 표시되는 'MEAL NOTE 설치'도 사용할 수 있습니다.

## 기존 기록
같은 Android Chrome·같은 웹 주소에서 설치하면 기존 localStorage를 유지하는 방식입니다.
PC나 다른 브라우저, 다른 웹 주소의 기록은 자동으로 따라오지 않습니다.
주소를 바꿔야 한다면 기존 앱에서 백업을 저장하고 새 주소의 설정에서 불러오세요.
설치해도 클라우드 백업이 생기는 것은 아닙니다. 브라우저 데이터를 직접 삭제하면 기록이 사라질 수 있습니다.

## 확인 결과
- 기존 기능·저장 관련 자동 검사 21개 통과.
- PWA 구성 검사 6개 통과: standalone 설정, 아이콘 실제 크기, manifest 연결, 등록/설치 처리, 오프라인 대상 파일, 서비스 워커의 사용자 저장 삭제 없음.
- 배포용 앱 파일 12개가 v20 원본과 동일한 것을 확인.
- 실제 HTTPS 배포 및 Android 단말 설치는 아직 수행하지 않았습니다.

